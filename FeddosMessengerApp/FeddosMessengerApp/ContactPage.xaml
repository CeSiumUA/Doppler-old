<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Doppler.ContactPage">
    <ContentPage.Content>
        <StackLayout>
            <Image x:Name="ContactImage"/>
            <Label Text="Ім'я"/>
            <Entry IsReadOnly="True" Text="{Binding Name}" FontSize="Medium"/>
            <Label Text="Логін"/>
            <Entry IsReadOnly="True" Text="{Binding CallName}" FontSize="Medium"/>
            <Label Text="Опис"/>
            <Entry IsReadOnly="True" Text="{Binding Description}" FontSize="Medium"/>
            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                <Button Text="Додати" Command="{Binding ListViewModel.SaveContactCommand}" CommandParameter="{Binding}"/>
                <Button Text="Написати" Command="{Binding ListViewModel.CreateChat}" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>